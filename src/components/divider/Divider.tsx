import { Divider as HerouiDivider } from "@heroui/react";
import clsx from "clsx";

export function DividerLevel1({ className }: { className?: string }) {
  return <HerouiDivider className={ className} />;
}   